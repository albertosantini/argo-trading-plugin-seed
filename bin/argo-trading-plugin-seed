#!/usr/bin/env node

"use strict";

var util = require("util"),
    StreamingNode = require("flic").node;

var streamingNode = new StreamingNode(function (err) {
    if (!err) {
        util.log("plugin online");
    } else {
        util.log(err);
    }
});

streamingNode.on("argo.streaming", function (data) {
    util.log(data);
});
